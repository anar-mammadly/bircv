/**
 * Default profile photo used by every sample / demo CV (home page, template gallery, "load sample", empty-editor preview).
 * `default-profile.jpg` is the untouched original (2048×2048); the demo CVs render a 720×720 downscale of the same
 * picture (same composition, no edits) so previews and the PDF stay light. A photo uploaded by the user always wins.
 */
export const DEFAULT_PROFILE_PHOTO = '/images/default-profile-720.jpg';
